<button style="width:80vw" class='goodbutton' hover-class="hover" bindtap="returnTap">返回主界面</button>
<button style="width:80vw" class='goodbutton1' hover-class="hover" bindtap="list">生成故障报表</button>

<view class='history-table-wrap'>
  <view class="table">
      <view class="tr">
          <view class="th th1">预测结果</view>
      </view>
      <view class="tr" wx:for="{{resultArray}}">
        <view class='"td' wx:if="{{index<=(endnumber-starnumber)}}">TEXT{{index*1+starnumber*1}}的预测结果为{{item}}</view>
      </view>
  </view>
</view>